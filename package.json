{
  "name": "web-tracking-cicd",
  "version": "1.0.0",
  "description": "Analytics CI/CD pipeline — spec generation, doc generation, dataLayer validation, GTM config generation",
  "type": "module",
  "scripts": {
    "test": "playwright test --config=playwright.config.js",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "yaml": "^2.8.4"
  }
}
