name: Spec Lint
# Triggered on every PR. Lints all specs in specs/, reporting success even when
# the diff touches no spec files — required so branch protection always sees a status.
on:
  pull_request:
jobs:
  lint:
    name: Validate tracking specs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Lint all specs in specs/
        run: |
          set -e
          FAILED=0
          SPECS=$(find specs/ -name "*.yaml" | sort)
          if [ -z "$SPECS" ]; then
            echo "No spec files found in specs/ — skipping."
            exit 0
          fi
          for spec in $SPECS; do
            echo ""
            echo "──────────────────────────────────────"
            echo "Linting: $spec"
            echo "──────────────────────────────────────"
            node scripts/linter/spec-linter.js "$spec" || FAILED=1
          done
          echo ""
          if [ $FAILED -eq 1 ]; then
            echo "❌ One or more specs failed validation. Merge blocked."
            exit 1
          else
            echo "✅ All specs passed validation."
          fi
